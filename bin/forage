#!/usr/bin/env node

program = require('commander'),
program
  .version('0.2.3')
  .option('-p, --port <port>', 'specify the port, defaults to 3000', Number, 3000)
  .parse(process.argv);
require('../lib/forage.js')({'port': program.port});

